package com.example.hotrepo.ui.trendingRepos

import androidx.test.ext.junit.runners.AndroidJUnit4
import androidx.test.filters.MediumTest
import org.junit.runner.RunWith

/**
 * Hilt does not currently support FragmentScenario because there is no way to specify an activity class,
 * and Hilt requires a Hilt fragment to be contained in a Hilt activity.
 * One workaround for this is to launch a Hilt activity and then attach your fragment.
 * All Relevant test cases are written in RepoActivity Test for Repo Fragment test
 * */

@MediumTest
@RunWith(AndroidJUnit4::class)
class RepoFragmentTest{
    /**
     * Hilt does not currently support FragmentScenario because there is no way to specify an activity class,
     * and Hilt requires a Hilt fragment to be contained in a Hilt activity.
     * One workaround for this is to launch a Hilt activity and then attach your fragment.
     * All Relevant test cases are written in RepoActivity Test for Repo Fragment test
     * */
}